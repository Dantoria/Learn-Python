#list method

basket = [1, 2, 3, 4, 5]

basket.extend([100])

basket.remove(3)
print(basket)

# append = add at end
# insert = add at position
# remove = remove by value
# pop = remove by index
# clear = remove all